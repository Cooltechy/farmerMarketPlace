# Production Environment Variables Template
# Copy this file to .env for production deployment

# REQUIRED: Set this to production for cloud deployment
NODE_ENV=production

# REQUIRED: Port (use process.env.PORT for cloud platforms)
PORT=3000

# REQUIRED: MongoDB Atlas connection string
# Replace with your production MongoDB Atlas URL
MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/agriconnect?retryWrites=true&w=majority

# REQUIRED: Session secret (NEVER share this publicly)
# Generate new one for production: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=generate-new-secret-for-production

# Security settings
BCRYPT_ROUNDS=12

# Optional: Additional security headers (for advanced deployment)
# TRUST_PROXY=true
# SECURE_COOKIES=true